import { createDisabledRoute } from '@/app/api/disabledRoute';

export const GET = createDisabledRoute('Google Play notifications');
export const POST = createDisabledRoute('Google Play notifications');
