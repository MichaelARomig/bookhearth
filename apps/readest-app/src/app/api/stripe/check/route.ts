import { createDisabledRoute } from '@/app/api/disabledRoute';

export const GET = createDisabledRoute('Stripe checkout validation');
export const POST = createDisabledRoute('Stripe checkout validation');
