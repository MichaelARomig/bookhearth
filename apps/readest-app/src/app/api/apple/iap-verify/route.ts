import { createDisabledRoute } from '@/app/api/disabledRoute';

export const GET = createDisabledRoute('App Store purchase verification');
export const POST = createDisabledRoute('App Store purchase verification');
