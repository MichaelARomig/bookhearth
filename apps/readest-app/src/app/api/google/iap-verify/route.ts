import { createDisabledRoute } from '@/app/api/disabledRoute';

export const GET = createDisabledRoute('Google Play purchase verification');
export const POST = createDisabledRoute('Google Play purchase verification');
